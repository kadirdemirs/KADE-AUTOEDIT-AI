# Esnek sürüm aralıkları — Windows, macOS (Apple Silicon/Intel) ve Linux'ta
# çalışacak şekilde. Önerilen Python: 3.10–3.12 (whisper/numpy tekerlekleri en
# stabil bu aralıkta). Python 3.13/3.14 da çalışır ama bazı paketler kaynaktan
# derlenmek zorunda kalabilir.

fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=12.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
sqlalchemy>=2.0,<3.0

# AI / ses-görüntü analizi
openai-whisper>=20231117
librosa>=0.10
ffmpeg-python>=0.2
opencv-python>=4.9
# pip paketinin adi 'scenedetect' (import scenedetect). 'PySceneDetect' eski/yanlis ad.
scenedetect>=0.6.3
pyloudnorm>=0.1.1
soundfile>=0.12.1
Pillow>=10.0          # offline meme uretimi (impact-font caption)

# numpy: whisper/librosa/scipy ile uyumlu 2.x serisi. Eski pinli 1.26.4 Apple
# Silicon + yeni Python'da tekerlek bulamayıp derlenmeye çalışıyordu.
numpy>=1.26,<3.0
scipy>=1.11

python-multipart>=0.0.9
aiofiles>=23.2

# Test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
