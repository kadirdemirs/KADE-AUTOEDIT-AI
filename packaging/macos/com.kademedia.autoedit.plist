<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  LaunchAgent for the KADE AutoEdit backend. Installed to
  ~/Library/LaunchAgents/com.kademedia.autoedit.plist by the .app launcher so the
  backend runs in the background on every login — the user never starts a server.
  __BACKEND_PATH__ is replaced at install time with the bundled kade-backend path.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.kademedia.autoedit</string>
    <key>ProgramArguments</key>
    <array>
        <string>__BACKEND_PATH__</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <false/>
    <key>ProcessType</key>
    <string>Background</string>
</dict>
</plist>
